meal {
    meal GET include recipe obj
}

user_attributes {
    email
    nurture workflow state
}

tags {
}

ingredients {
    ingredient_id pk
    meal_id fk
    ingredient_name
}

still issue with create tag returns user_id is null

make (meal && recipe).create router.push() go to detail view not logs
avg cook time can be computed frontend not stored in db